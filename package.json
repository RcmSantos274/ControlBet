{
  "name": "controlbet",
  "version": "1.0.0",
  "scripts": {
    "build": "cd site && npm install && npm run build && cd .. && node -e \"require('fs').cpSync('./site/out','./backend/public',{recursive:true,force:true})\" && cd backend && npm install && npx prisma generate && npx tsc",
    "start": "cd backend && node dist/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

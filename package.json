{
  "name": "controlbet",
  "version": "1.0.0",
  "scripts": {
    "build": "cd site && npm install && npm run build && cd ../backend && npm install && npm run build",
    "start": "cd backend && node dist/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
